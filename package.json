{
  "name": "geotest",
  "version": "1.0.0",
  "description": "Un joc curt de geografia de Catalunya.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && node scripts/serve.mjs",
    "start": "npm run dev",
    "test": "node --test"
  },
  "author": "TelesforoAleix",
  "license": "ISC",
  "engines": {
    "node": ">=20"
  }
}
